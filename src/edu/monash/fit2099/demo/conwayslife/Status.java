package edu.monash.fit2099.demo.conwayslife;

/**
 * an enum type to check if a actor is dead or alive
 */
public enum Status {
	/**
	 * the enum value to state if the actor with this in his capabilities is alive
	 */
	ALIVE,
	/**
	 * the enum value to state if the actor with this in his capabilities is dead
	 */
	DEAD
}
